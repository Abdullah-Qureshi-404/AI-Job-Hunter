# Use the public anon key from Supabase Dashboard → Project Settings → API.
# Do NOT paste the service_role key here (it must stay server-only).
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

VITE_API_URL=http://localhost:8000
